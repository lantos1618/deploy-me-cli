# deploy-me-cli — moved

This repo has moved into the [`deploy-me-packages`](https://github.com/lantos1618/deploy-me-packages) monorepo. The CLI now lives at `packages/cli/` and ships to npm as **`dp`**.

Install:
```
npm install -g dp
```

Issues + PRs: https://github.com/lantos1618/deploy-me-packages

---
*Archived 2026-05-15. Full commit history preserved in the monorepo via `git subtree`.*
